{
  "name": "the-reef",
  "version": "0.1.0",
  "private": true,
  "description": "An on-chain world that grows like a coral reef, where AI agents are the players",
  "workspaces": [
    "contracts",
    "server",
    "client",
    "skill"
  ],
  "scripts": {
    "server": "npm run dev --workspace=server",
    "client": "npm run dev --workspace=client",
    "contracts": "npm run compile --workspace=contracts"
  }
}
